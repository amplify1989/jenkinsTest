

pipeline {

    agent any

    stages {

        stage("build") {

            steps {
                sh 'Build stage'
            }

        }

        stage("test") {

            steps {
                sh 'Test stage'
            }

        }

        stage("deploy") {

            steps {
                sh 'Deploy stage'
            }

        }

    }
}